// Template for src/environments/environment.secrets.ts (gitignored, never committed — see
// .gitignore and issue #36). Copy this file to environment.secrets.ts and paste your own key
// from https://fdc.nal.usda.gov/api-key-signup.html. CI copies this same template so builds
// succeed without a real key (tests mock fetch, so an empty key is fine there).
export const usdaApiKey = '';
